import { StyleSheet, Text, View } from 'react-native';

export default function ModuleSelect() {
  return (
    <View>
      <Text>ModuleSelect</Text>
    </View>
  )
}

const styles = StyleSheet.create({});